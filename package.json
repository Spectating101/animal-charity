{
  "name": "animal-feed-relief-network",
  "version": "0.1.0",
  "private": true,
  "description": "Validation-first coordination system for safe, traceable animal-feed relief.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "check": "node --check src/server.js && node --check src/rules.js && node --check src/workflow.js && node --check public/app.js"
  }
}
